---
# YAML Front Matter：文章設定區塊
title: "第2篇紀錄：Jekyll與GitHub"
description: 花了六個小時跟Gemini雞同鴨講，最後多次排除問題才把這個網站架起來。AI的確能處理程式碼的語法問題，但在結合介面操作上，可能沒辦法那麼一次到位。例如，怎麼去編輯程式碼，求快就教你使用nano，但是真的管用還是使用VS code 一次整個編輯。最後，使用者必須還是要會看懂一些程式碼，你才會知道AI哪邊是他的極限了。這篇教學照著做可能還是會有問題，目前還沒有時間一一排除。
categories: ["Vibe Coding",'GitHub']   # 這是您的分類，可以自己決定
tags: ["Jekyll","Gemini"]           # 這是文章的標籤
---

# 🚀 從零開始：打造 Jekyll 靜態網站並部署到 GitHub Pages

您是否想擁有一個快速、安全且完全免費的個人部落格或網站？**Jekyll** 配合 **GitHub Pages** 是業界最流行的選擇之一。本文將帶您從本地環境設定開始，一直到解決部署過程中遇到的實際問題，最終成功發布您的網站！

## 一、環境準備與本地建置

Jekyll 是一個 Ruby Gem，因此您需要準備 Ruby 環境。

### 1. 安裝 Ruby, Gem 與 Jekyll

Jekyll 依賴於 **Ruby** 環境。在 macOS 或 Linux 上，通常已經預裝了 Ruby。

* **安裝 Bundler:** Bundler 是一個 Ruby 依賴管理工具，能確保您的專案使用正確版本的 Gem。
    ```bash
    gem install bundler
    ```
* **建立新的 Jekyll 專案 (使用 Minima 主題):**
    ```bash
    jekyll new my-awesome-blog
    cd my-awesome-blog
    ```
* **安裝專案依賴:**
    ```bash
    bundle install
    ```


### 2. 本地預覽與測試

在部署到 GitHub 前，強烈建議在本地運行您的網站，以捕捉任何編譯錯誤。

* **啟動本地伺服器:**
    ```bash
    bundle exec jekyll serve
    ```
* **訪問:** 打開瀏覽器，訪問 `http://localhost:4000` 即可看到您的網站。

---

## 二、Git 流程與 GitHub Pages 部署

將本地網站上線的關鍵在於正確配置 GitHub 倉庫，並處理好 Git 推送流程。

### 1. 關鍵設定：建立倉庫與 Git 初始化

#### A. 建立 GitHub 倉庫 (Repository)

要使用 GitHub Pages，您的倉庫命名至關重要：

* **個人網站 (User Site):** 倉庫名稱必須精確匹配您的用戶名：`YourUsername.github.io`。
    * *範例：* 如果您的用戶名是 `JasonGit000`，倉庫名稱必須是 `JasonGit000.github.io`。

#### B. 配置 Git 遠端和身份

在您的專案目錄中，初始化 Git 並連結到您的 GitHub 倉庫：

```bash
git init
git remote add origin [https://github.com/YourUsername/YourUsername.github.io.git](https://github.com/YourUsername/YourUsername.github.io.git)
```

### 推送至 GitHub

這是將您的本地內容同步到線上的步驟。
```Bash
git add .
git commit -m "Initial commit of Jekyll site"
git push origin main # 根據您的預設分支，可能是 master
```

### 難題一：部署成功但網站 404 或內容錯誤

問題描述： GitHub Actions 顯示綠色成功，但訪問 https://YourUsername.github.io/ 卻是 404，或者內容顯示是舊的/預設的。

根本原因： 倉庫命名衝突導致部署路徑錯誤，以及伺服器快取問題。

| 錯誤現象 | 錯誤原因 | 解決方案 |
| :--- | :--- | :--- |
| **網址是 `.../RepoName/`** | 倉庫名與用戶名不匹配，被視為 **Project Site**。 | **解決步驟：** 將倉庫名稱從 `RepoName.github.io` **重命名**為 `YourUsername.github.io`。 |
| **`Your awesome title`** | 本地有修改，但尚未推送到 GitHub。 | **解決步驟：** 執行 `git push`，將本地提交同步到遠端。 |
| **內容已推送，但仍是舊的** | GitHub Pages 伺服器的 CDN (快取) 問題。 | **解決步驟：** 進入 **Settings -> Pages**，將部署分支臨時切換到另一個分支再切換回來，**強制清除快取並重新部署**。 |
| **圖片顯示問號 (404)** | Markdown 中引用的路徑與圖片實際存放的路徑不匹配。 | **最保險的引用方式 (使用 Liquid):** `![描述]({{ site.baseurl }}/assets/images/圖片名稱.jpg)` |
| **`Your branch is ahead`** | 本地提交了 (committed)，但沒有推送 (pushed)。 | **解決步驟：** 執行 `git push origin master` 將最新工作內容上傳。 |

